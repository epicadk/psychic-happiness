rootProject.name = "fitness-nutrition-tracker"

pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

// Phase 0: the shared KMP module. Platform app modules (:androidApp, :iosApp)
// are added in Phase 1.
include(":shared")
